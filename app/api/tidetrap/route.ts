import{NextResponse}from'next/server';import{store}from'@/lib/store';export async function GET(){return NextResponse.json(store().nodes)}export async function POST(req:Request){const b=await req.json();const n=store().nodes.find(x=>x.id===b.id);if(!n)return NextResponse.json({error:'node not found'},{status:404});Object.assign(n,{fill:b.fill??n.fill,collectedToday:b.collectedToday??n.collectedToday,status:b.status??n.status,lastSeen:'just now',trend:b.trend??n.trend});return NextResponse.json(n)}