import{NextResponse}from'next/server';import{dashboard}from'@/lib/engine';export async function GET(req:Request){const d=dashboard();const format=new URL(req.url).searchParams.get('format')||'json';if(format!=='csv')return NextResponse.json(d);const rows=['date,waste_kg,collections',...d.history.map(h=>`${h.date},${h.kg},${h.collections}`)];return new NextResponse(rows.join('\n'),{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="wastelens-report.csv"'}})}