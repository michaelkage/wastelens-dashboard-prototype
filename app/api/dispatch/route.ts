import{NextResponse}from'next/server';import{store}from'@/lib/store';export async function POST(req:Request){const b=await req.json();const id=`dispatch-${Date.now()}`;store().activity.unshift({id,type:'dispatch',message:`Dispatch sent for ${b.massKg}kg ${b.material} to ${b.destination}`,time:new Date().toISOString()});return NextResponse.json({ok:true,provider:'MockSMSProvider',message:`SMS dispatch recorded. ${b.massKg}kg ${b.material} pickup scheduled for ${b.destination}.`})}