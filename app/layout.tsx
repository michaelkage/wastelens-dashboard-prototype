import type{Metadata,Viewport}from'next';import'./globals.css';export const metadata:Metadata={title:'WasteLens // AI Analytics',description:'Waste intelligence for Terranova, a Junior Achievement Nigeria innovation project.'};export const viewport:Viewport={colorScheme:'light',themeColor:'#123d2b'};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body className="antialiased">{children}</body></html>}